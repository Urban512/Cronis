package dev.cronis.gui.component;

/**
 * Container component that groups related content within a visually distinct surface.
 * <p>
 * Cards establish hierarchy and spacing between sections without coupling screens
 * to bespoke layout code.
 */
public class GuiCard extends GuiComponent {
	public GuiCard() {
	}
}
