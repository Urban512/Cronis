package dev.cronis.gui.component;

/**
 * Interactive component for triggering a single primary action.
 * <p>
 * Buttons share the global Cronis visual language and are reused across screens
 * instead of ad hoc click targets.
 */
public class GuiButton extends GuiComponent {
	public GuiButton() {
	}
}
