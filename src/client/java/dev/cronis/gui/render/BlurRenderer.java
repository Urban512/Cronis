package dev.cronis.gui.render;

/**
 * Handles backdrop blur effects for layered Cronis interface surfaces.
 * <p>
 * Blur rendering is isolated so screens and components can request depth without
 * owning low-level draw logic.
 */
public class BlurRenderer {
	public BlurRenderer() {
	}
}
